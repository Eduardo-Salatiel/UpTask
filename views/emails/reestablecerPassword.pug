<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style
            include ./email.css
    body
        h2 Reestablecer Password
        p Hola ¿Haz solicitado cambiar tu password? da click en el siguiente enlace para reestablcerlo (Este enlace es temporal)
        a(href=`${resetUrl}`) Ir a resetear el password 
        p Si no puedes acceder al enlace visita: #{resetUrl}